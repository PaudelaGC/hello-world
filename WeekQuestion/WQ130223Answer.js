function imprimirDeZeroFinsCen(){
    let dos = [0,0]
    let cinc = [0,0,0,0,0]
    let max = Math.pow(dos.length,dos.length)*Math.pow(cinc.length,dos.length);
    for(let i = 0; i<=max; i++){
        console.log(i);
    }
}
imprimirDeZeroFinsCen();
